import React, { PropTypes } from 'react'

class Graphs extends React.Component {
  render () {
    return <span>Graphs</span>
  }
}

const styles = {
  container: {
    width: 500
  }
}
export default Graphs;
